{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "CI=false NODE_OPTIONS=--openssl-legacy-provider craco build"
}
